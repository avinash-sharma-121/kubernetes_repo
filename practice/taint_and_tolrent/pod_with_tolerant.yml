apiVersion: v1
kind: Pod
metadata:
  name: tolerant-pod
  namespace: testing
spec:
  containers:
    - name: nginx
      image: nginx:latest
  tolerations:
    - key: "dev"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"
  

